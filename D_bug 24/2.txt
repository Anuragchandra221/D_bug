The C program given calculates the sum of even and odd numbers separately up to 20
Eg. input - 20
output
Sum of even numbers up to 20: 110
Sum of odd numbers up to 20: 100
The following code contains both logical and syntactical errors.
Find it and fix it !!!

#include <stdio.h>
    int main() {
    int 1;
    int sum_even = 0, sum_odd = 0;
    for (i = 1; i <= 20; i++){
        if (i % 2 == o) {
            sum_even += i:
        }else{
            sum_odd += i:
        }
    }
    printf("Sum of even numbers up to 20: %d\n", sum_even);
    printf("Sum of odd numbers up to 20: %d\n", sum_odd);
    return o;
}